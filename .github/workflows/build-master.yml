name: Build Master

# Every push to main publishes moving dev tags (never `latest`).
on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/_shared-build.yml
    with:
      ref: ${{ github.sha }}
      tags: '["main","unstable"]'
    secrets:
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
